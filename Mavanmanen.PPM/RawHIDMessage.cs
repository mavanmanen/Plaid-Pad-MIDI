namespace Mavanmanen.PPM
{
    public enum RawHIDMessage
    {
        HID_ACK = 100,
        HID_CONNECTED,
        HID_DISCONNECTED,
        HID_GET_STATE,
        HID_SAVE_STATE,
        HID_STATE,
        HID_CLEAR_STATE
    }
}
